[package]
name = "devdash"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "net"] }
askama = "0.12"
sysinfo = "0.30"
tower-http = { version = "0.6", features = ["fs"] }